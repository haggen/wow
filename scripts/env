#!/usr/bin/env bash

set -euo pipefail

tag="${GITHUB_REF#refs/tags/}"
target="${tag%/*}"
release="$(scripts/inspect "$target")"

cat <<-EOF > "$GITHUB_ENV"
	TAG=$tag
	TARGET=$target
	RELEASE=$release
EOF